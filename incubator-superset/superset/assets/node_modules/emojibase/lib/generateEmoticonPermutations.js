"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = generateEmoticonPermutations;

var _toConsumableArray2 = _interopRequireDefault(require("@babel/runtime/helpers/toConsumableArray"));

function generateEmoticonPermutations(emoticon) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var _options$isFace = options.isFace,
      isFace = _options$isFace === void 0 ? true : _options$isFace,
      _options$withNose = options.withNose,
      withNose = _options$withNose === void 0 ? true : _options$withNose;
  var list = [emoticon];

  if (emoticon.toUpperCase() !== emoticon) {
    list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.toUpperCase(), options)));
  }

  if (isFace) {
    if (emoticon.indexOf('/') >= 0) {
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace('/', '\\'), options)));
    }

    if (emoticon.indexOf(')') >= 0) {
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace(')', ']'), options)));
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace(')', '}'), options)));
    }

    if (emoticon.indexOf('(') >= 0) {
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace('(', '['), options)));
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace('(', '{'), options)));
    }

    if (emoticon.indexOf(':') >= 0) {
      list.push.apply(list, (0, _toConsumableArray2.default)(generateEmoticonPermutations(emoticon.replace(':', '='), options)));
    }

    if (withNose) {
      list.forEach(function (emo) {
        if (emo.indexOf('-') === -1) {
          list.push("".concat(emo.slice(0, emo.length - 1), "-").concat(emo.slice(-1)));
        }
      });
    }
  }

  list.sort(function (a, b) {
    return b.length - a.length;
  });
  return Array.from(new Set(list));
}