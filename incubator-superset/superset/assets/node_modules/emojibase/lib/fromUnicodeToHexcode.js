"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = fromUnicodeToHexcode;

var _constants = require("./constants");

function fromUnicodeToHexcode(unicode) {
  var strip = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var hexcode = [];
  Array.from(unicode).forEach(function (codepoint) {
    var hex = codepoint.codePointAt(0).toString(16).toUpperCase();

    while (hex.length < 4) {
      hex = "0".concat(hex);
    }

    if (!strip || strip && !hex.match(_constants.SEQUENCE_REMOVAL_PATTERN)) {
      hexcode.push(hex);
    }
  });
  return hexcode.join('-');
}