export default function fromHexcodeToCodepoint(code) {
  var joiner = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '-';
  return code.split(joiner).map(function (point) {
    return parseInt(point, 16);
  });
}