import { QueryFormDataMetric } from '@superset-ui/query';
import { PlainObject } from './types';
export default function processData({ timeseriesLimitMetric, orderDesc, records, metrics, }: {
    timeseriesLimitMetric: QueryFormDataMetric;
    orderDesc: boolean;
    records: PlainObject[];
    metrics: string[];
}): {
    data: {
        [x: string]: any;
    };
}[];
