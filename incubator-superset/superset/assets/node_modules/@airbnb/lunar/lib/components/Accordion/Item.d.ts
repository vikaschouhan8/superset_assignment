import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** Apply a border. */
    bordered?: boolean;
    /** Content to render if the accordion item is expanded. */
    children?: React.ReactNode;
    /** Whether the accordion item is expanded or not. */
    expanded?: boolean;
    /** Unique id of the accordion item. */
    id: string;
    /** Index amongst a collection of accordion items. */
    index?: number;
    /** Title of the accordion item. */
    title?: React.ReactNode;
    /** Callback fired when the accordion item is clicked. */
    onClick?: (index: number) => void;
};
/**
 * A single accordion item. To be rendered amongst a collection of accordion items, within an accordion.
 */
export declare class AccordionItem extends React.Component<Props & WithStylesProps> {
    static defaultProps: {
        bordered: boolean;
        children: null;
        expanded: boolean;
    };
    private handleClick;
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=Item.d.ts.map