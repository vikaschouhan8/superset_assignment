import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** Display in the center horizontally. */
    centerAlign?: boolean;
    /** Content to render. */
    children?: React.ReactNode;
    /** Span multiple columns. */
    colSpan?: number;
    /** Display horizontally at the end. */
    endAlign?: boolean;
    /** Render as a table header. */
    header?: boolean;
    /** Display horizontally at the start. */
    startAlign?: boolean;
    /** Truncate text and display all on hover. */
    truncate?: boolean;
    /** Wrap text and white space. */
    wrap?: boolean;
};
/** An individual table cell. */
export declare class TableCell extends React.Component<Props & WithStylesProps> {
    static defaultProps: {
        centerAlign: boolean;
        endAlign: boolean;
        header: boolean;
        startAlign: boolean;
        truncate: boolean;
        wrap: boolean;
    };
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=Cell.d.ts.map