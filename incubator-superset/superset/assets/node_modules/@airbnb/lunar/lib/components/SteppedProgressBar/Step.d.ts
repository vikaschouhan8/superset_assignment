import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** Mark the step as complete. */
    complete?: boolean;
    /** @ignore Mark as the first step. */
    first?: boolean;
    /** A label to display within a tooltip. */
    label?: React.ReactNode;
    /** @ignore Mark as the last step. */
    last?: boolean;
};
/** An individual step within a stepped progress bar. */
export declare class Step extends React.Component<Props & WithStylesProps> {
    static defaultProps: {
        complete: boolean;
        first: boolean;
        label: null;
        last: boolean;
    };
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=Step.d.ts.map