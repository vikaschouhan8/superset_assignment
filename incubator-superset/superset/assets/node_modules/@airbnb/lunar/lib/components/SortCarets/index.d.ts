import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** Whether or not to display the bottom caret. */
    down?: boolean;
    /** If enabled, the caret is more pronounced. */
    enableDown?: boolean;
    /** If enabled, the caret is more pronounced. */
    enableUp?: boolean;
    /** Whether or not to display the top caret. */
    up?: boolean;
};
/** Carets to indicate sorting on a table. */
export declare class SortCarets extends React.Component<Props & WithStylesProps> {
    static defaultProps: {
        down: boolean;
        enableDown: boolean;
        enableUp: boolean;
        up: boolean;
    };
    renderCaretUp(): false | JSX.Element | undefined;
    renderCaretDown(): false | JSX.Element | undefined;
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=index.d.ts.map