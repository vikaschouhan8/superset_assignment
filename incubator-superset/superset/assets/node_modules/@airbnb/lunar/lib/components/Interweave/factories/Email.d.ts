import React from 'react';
import { EmailProps } from 'interweave-autolink';
export declare type Props = EmailProps & {
    /** Render using large prop on the Link component. */
    large?: boolean;
    /** Render using small prop on the Link component. */
    small?: boolean;
};
export default class Email extends React.PureComponent<Props> {
    static propTypes: {};
    static defaultProps: {
        large: boolean;
        newWindow: boolean;
        small: boolean;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=Email.d.ts.map