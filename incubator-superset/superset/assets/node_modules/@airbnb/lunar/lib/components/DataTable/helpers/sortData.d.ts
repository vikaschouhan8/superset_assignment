import { SortDirectionType } from 'react-virtualized';
import { GenericRow, SelectedRows } from '../types';
export default function sortData<T extends GenericRow>(list: T[], keys: string[], selectedRows: SelectedRows, selectedRowsFirst: boolean, sortBy?: string, sortDirection?: SortDirectionType): T[];
//# sourceMappingURL=sortData.d.ts.map