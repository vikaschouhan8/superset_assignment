import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
import Cell from './Cell';
import Row from './Row';
export declare type Props = {
    /** Apply a wrapping border. */
    bordered?: boolean;
    /** Table head, body, foot, and rows. */
    children: NonNullable<React.ReactNode>;
    /** Reduce padding within cells. */
    compact?: boolean;
    /** Disable automatic cell scaling. */
    fixed?: boolean;
    /** Apply a horizontal border between rows. */
    horizontal?: boolean;
    /** Mark as loading and disable interaction. */
    loading?: boolean;
    /** Applies verticalAlign: middle to child cells. */
    middleAlign?: boolean;
    /** Alternate row background color. */
    striped?: boolean;
    /** A unique name for tracking purposes. */
    trackingName?: string;
    /** Render with a transparent background. */
    transparent?: boolean;
    /** Apply a vertical border between rows. */
    vertical?: boolean;
};
/** A responsive table for displaying tabular data. */
export declare class Table extends React.Component<Props & WithStylesProps> {
    static defaultProps: {
        bordered: boolean;
        compact: boolean;
        fixed: boolean;
        horizontal: boolean;
        loading: boolean;
        middleAlign: boolean;
        striped: boolean;
        transparent: boolean;
        vertical: boolean;
    };
    render(): JSX.Element;
}
export { Cell, Row };
declare const _default: React.ComponentType<Props & import("aesthetic-react").WithStylesWrapperProps & import("../../composers/withBoundary").WithBoundaryWrapperProps>;
export default _default;
//# sourceMappingURL=index.d.ts.map